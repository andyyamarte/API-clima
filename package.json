{
  "name": "api_clima",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate-config": "node scripts/generate-config.js",
    "build": "npm run generate-config && tsc",
    "dev": "npm run generate-config && tsc --watch",
    "prepare-pages": "npm run build && node scripts/prepare-pages.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
